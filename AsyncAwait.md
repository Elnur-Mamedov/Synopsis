`async` - пишетс€ перед методами и дает программе пон€ть что 
метод асинхронный, то есть его нужно подождать пока он не выполнит свою работу
и не вернет результат своей работы.

`await` - говорит подождать потоку, пока метод не выполнитс€
пишетс€ перед вызовом асинхронного метода.

ћожно пон€ть что метод асинхронный, если в конце функции `Async`
и в основном он возвращает `Task`.

ќбычный `Task` который возвращает метод, это как `void`, то есть
метод ничего не возвращает. Ќо если вы все же хотите что-то вернуть
можно написать `Task<string>` в <“ип>. “о есть вы говорите что
метод вернет обешание в котором есть строка.

¬ методах нельз€ писать `async void`, но в соб€ти€х можно.
—обыти€ это те которые например нажимаешь на кнопку и вызываетс€
событие, например выводитс€ какое то сообщение.